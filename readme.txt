This folder contains both the dataset files. Other files with description are as follows:
201601248_lab02.py-main file for reading the dataset, getting the frequency count and prediction
modules.py-file containing modules 'train' and 'predict' for getting the frequency count and getting the most probable class respectively.
Running the code:
just run the file in terminal with following command:
python 201601248_lab02.py
